<nav class="navbar navbar-fixed-top navbar-expand bg-primary">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item">
            <a class="nav-link" href="/"><i class="material-icons">home</i></a>
        </li>
        {{#tabs}}
            <li class="nav-item">
            	<table>
	            	<tbody>      	
		            	<tr>
				            <td class="text-center"><a class="nav-link" href="{{.}}">{{.}}</a></td>
				            <td class="td-actions text-right">
				                <a href="removeTab/{{.}}"><button class="btn btn-sm btn-primary" role="button">x</button></a>
				            </td>
			       		</tr>
		       		</tbody>
                </table>
            </li>
        {{/tabs}}
    </ul>
    <span class="navbar-item">
        <div class="container margin-right">
            {{#student}}
                <ul class="nav navbar-nav">
                    <li>
                        <form action="/logout" method="post">
                            <button class="btn btn-primary navbar-btn" type="submit">Log Out</button>
                            <input type="hidden" name="_csrf" value="{{token}}" />
                        </form>
                    </li>
                </ul>
            {{/student}}
            {{^student}}
                <button type="button" class="btn btn-default btn-link" data-toggle="modal" data-target="#login">
                    Login
                </button>
                {{/student}}
        </div>
    </span>
</nav>
